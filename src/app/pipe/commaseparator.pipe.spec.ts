import { CommaseparatorPipe } from './commaseparator.pipe';

describe('CommaseparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaseparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
